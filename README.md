## Инструкция по установке:

1. Склонируйте репозиторий и перейдите в папку проекта:

```bash
git clone <URL_репозитория>
cd delivery_service
```
2. создать собственное окружение, используйте .env.example как шаблон:

```bash
cp .env.example .env
```

3. Убедитесь, что установлен Docker и Docker Compose.

4. Запустите контейнеры

``` bash
docker-compose up -d
```

5. Примените миграции и суперпользователя

```bash
docker-compose exec web python manage.py migrate
docker-compose exec web python manage.py createsuperuser
```

6. Swagger / DRF API доступен по адресу:
 http://localhost:8001/api/docs/#/

7. Админ панель Джанго доступна по адресу: http://localhost:8001/admin/

## О проекте

**Delivery Service** — это масштабируемое REST API для e-commerce платформ (маркетплейсов), обеспечивающее управление полным циклом продаж: от ведения каталога товаров до автоматического расчета логистики через внешние службы доставки.

### Основной функционал:
* **Управление контентом:** полноценный CRUD для пользователей, магазинов (продавцов), карточек товаров и их уникальных торговых предложений (SKU).
* **Корзина и Заказы:** гибкая система управления заказами с возможностью группировки товаров от разных продавцов внутри одной сессии.
* **Автоматизация логистики:** интеграция с API внешних транспортных компаний (на базе расширяемой архитектуры адаптеров) для динамического расчета стоимости и сроков доставки в режиме реального времени.
* **Безопасность:** ролевая модель доступа (RBAC) для разграничения прав между Покупателями, Продавцами (владельцами магазинов) и Администраторами платформы.



# Описание общей архитектуры проекта
## Создание заказа в интернет-магазине

1. Пользователь добавляет товары в корзину (`OrderProduct`).
2. Пользователь нажимает **«Оформить заказ»**.
3. Создается сущность `Order`.
4. `OrderProduct` группируются по магазинам.
5. Для каждого магазина создается `OrderDelivery`.
## Выбор доставки

1. Для каждого `OrderDelivery` система получает доступные способы доставки.
2. Пользователь выбирает способ получения (ПВЗ или курьер).
3. Пользователь выбирает конкретный пункт выдачи или адрес доставки.
4. Система рассчитывает стоимость доставки и определяет подходящий тариф (`tariff_code`) для каждого `OrderDelivery`.
5. Пользователю отображается итоговая стоимость заказа:

```
Стоимость товаров
+ 
стоимость всех отправлений (OrderDelivery)
= 
итог к оплате
```

6. Пользователь подтверждает оформление заказа и производит оплату.

## Регистрация отправлений

1. После успешного оформления (или оплаты) для каждого `OrderDelivery` регистрируется отдельное отправление в API службы доставки.
2. Для каждого отправления сохраняются:

- `cdek_uuid`;
- номер отправления (`cdek_number`);
- стоимость доставки;
- текущий статус;
- данные для отслеживания.

```
	Order
│
├── OrderDelivery (Shop A)
│       │
│       ├── OrderProduct(1)
│       ├── OrderProduct(2)
│       │
│       ├── shop = Shop A
│       ├── delivery_type = CDEK
│       └── CdekDeliveryMeta
│               ├── pickup_point
│               ├── tariff_code
│               ├── cdek_uuid
│               ├── cdek_number
│               ├── shipment_price
│               └── order_status
│
├── OrderDelivery (Shop B)
│       │
│       ├── OrderProduct(3)
│       ├── OrderProduct(4)
│       │
│       ├── shop = Shop B
│       ├── delivery_type = CDEK
│       └── CdekDeliveryMeta
│               ├── pickup_point
│               ├── tariff_code
│               ├── cdek_uuid
│               ├── cdek_number
│               ├── shipment_price
│               └── order_status
│
└── OrderDelivery (Shop C)
        │
        ├── OrderProduct(5)
        │
        ├── shop = Shop C
        ├── delivery_type = CDEK
        └── CdekDeliveryMeta
                ├── pickup_point
                ├── tariff_code
                ├── cdek_uuid
                ├── cdek_number
                ├── shipment_price
                └── order_status
```

# Используемые паттерны


| Паттерн                               | Описание                                                                                                                                                                                                                                                                                                                                   | Где используется                                                                                                                                                                                                                                                                               |
| ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Singleton                             | Создается один объект через метод __ new__. Используется classmethod для создание одного экземпляра на всех.                                                                                                                                                                                                                               | единый клиент подключения к API СДЭК                                                                                                                                                                                                                                                           |
| Facade                                | абстрагирует более простой интерфейс от сложного                                                                                                                                                                                                                                                                                           | единая точка входа в модуль интеграции со СДЭК                                                                                                                                                                                                                                                 |
| Adapter                               | Единственной целью адаптера является обеспечение совместной работы двух объектов, даже если их интерфейсы несовместимы. Ведь адаптация может повлечь за собой множество задач, таких как преобразование арrументов в другой формат, изменение порядка аргументов, вызов метода с другим именем или предоставление аргументов по умолчанию. | поддержка различных служб доставки через общий интерфейс.<br>1. Совмещаем несколько видов доставки товара (СДЭК, "Деловые линии", Boxberry и т.п.)                                                                                                                                             |
| Strategy                              | сделать какое-либо действия разными способами в зависимости от формата  входящих данных. Например, отсортировать список: просто вызываем функцию sorted()                                                                                                                                                                                  | выбор алгоритма работы в зависимости от службы доставки или способа доставки                                                                                                                                                                                                                   |
| State                                 | структурно подобен паттерну Стратегия. Цель: представление систем переходов состояний - возможны только узко определённые переходы в, другие состояния. В, отличие от паттерна Стратегии предназначен для динамического переключения между различными состояниями по мере развития какого-либо процесса.                                   | управление жизненным циклом отправления и переходами между статусами                                                                                                                                                                                                                           |
| Observer                              | полезен для, сохранения промежуточных состояний объектов: у наблюдаемого должен быть список наблюдателей и способ присоединения новых, наблюдателей. Подклассы наблюдателей должны иметь метод __call__ - к нему обращается наблюдаемый для уведомления каждого наблюдателя об изменении состояния.                                        | обработка вебхуков и уведомление внутренних компонентов                                                                                                                                                                                                                                        |
| Decorator                             | используется:<br>1. При улучшении отклика, компонента при оправке данных второму компоненту (взаимодействие сокетов: например перед отправкой сообщения, пишется Лог) <br>2. Поддержка нескольких дополнительных вариантов поведения ( напротив перед отправкой совету сжимать данные)                                                     | логирование, обработка ошибок, повторные попытки, обновление OAuth-токена                                                                                                                                                                                                                      |
| Generator                             | Ленивая загрузка по одной сроке (например, большой объем логов), использование круглых скобок вместо {} bkb [].                                                                                                                                                                                                                            | потоковая обработка больших списков ПВЗ и тарифов                                                                                                                                                                                                                                              |
| Factory (Simple Factory)              | Инкапсулирует выбор и запуск логики, связанной с конкретной службой доставки. Клиентский код не содержит множества условных операторов (`if/else`) и обращается к единой точке входа. Фабрика определяет необходимый обработчик в зависимости от выбранного перевозчика.                                                                   | Инициализация и очистка службы доставки после создания, изменения или удаления магазина (`DeliveryFactory`). В зависимости от выбранного перевозчика запускается соответствующая логика (`initialize_cdek`, `cleanup_cdek`).                                                                   |
| Lazy Initialization + Shared Resource | Архитектурный подход, при котором общий ресурс создается и поддерживается только при наличии потребителей. Позволяет не выполнять лишние операции и не хранить данные, которые в данный момент не используются.                                                                                                                            | Общий справочник тарифов СДЭК. При создании первого магазина с перевозчиком СДЭК запускается синхронизация тарифов. Если магазинов СДЭК нет, Celery-задача завершается без обращения к API. При удалении последнего магазина СДЭК выполняется очистка общего кэша Redis и справочника тарифов. |


# Описание конкретной реализации модулей

## Система ролей и доступа

### 1. Общая схема авторизации

В системе используется ролевая модель управления доступом (RBAC — Role-Based Access Control).

Пользователь регистрируется в системе как обычный пользователь и получает базовую роль `User`. Для входа используется единый механизм авторизации по электронной почте и паролю.

После успешной авторизации сервер формирует JWT-токен и создает активную пользовательскую сессию. JWT-токен используется для доступа к защищенным API-эндпоинтам.

Отдельного входа для разных ролей нет.

Один пользователь может одновременно иметь несколько ролей. Например, пользователь маркетплейса, который создал собственный магазин, может иметь одновременно роли:

```
User
Seller
```

В таком случае один JWT-токен используется для доступа ко всем разрешенным действиям обеих ролей.

### 2. Регистрация пользователя

Регистрация выполняется через публичный endpoint:

```
POST /api/users/register/
```

Для регистрации передаются данные пользователя, например:

```
{
    "email": "user@example.com",
    "password": "12345678",
    "password_repeat": "12345678",
    "first_name": "Ivan",
    "last_name": "Ivanov",
    "middle_name": "Ivanovich"
}
```

После успешной регистрации создается пользователь.

По умолчанию пользователь является обычным пользователем маркетплейса и получает роль `User`.

Роль `Seller` при обычной регистрации не назначается.

### 3. Авторизация пользователя

Вход в систему выполняется через единый endpoint:

```
POST /api/users/login/
```

Пример запроса:

```
{
    "email": "user@example.com",
    "password": "12345678"
}
```

При успешной авторизации выполняются следующие действия:

1. проверяется email и пароль;
2. определяется пользователь;
3. формируется JWT-токен;
4. создается активная запись `Session`;
5. клиенту возвращается JWT-токен и информация о пользователе.

Пример ответа:

```
{
    "token": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...",
    "user_id": 1,
    "email": "user@example.com"
}
```

Для последующих запросов токен передается в HTTP-заголовке:

```
Authorization: Bearer <JWT_TOKEN>
```

### 4. Роли системы

В системе используются четыре основные роли:

| Роль   | Назначение                                |
| ------ | ----------------------------------------- |
| Admin  | Администрирование системы и полный доступ |
| Seller | Управление собственным магазином          |
| User   | Покупатель маркетплейса                   |
| Guest  | Неавторизованный пользователь             |
Отдельный аккаунт или отдельный способ авторизации для `Seller` не требуется.

### 5. Роль User

`User` — базовая роль зарегистрированного пользователя маркетплейса.

Пользователь может:
- просматривать магазины;
- просматривать товары;
- создавать собственные заказы;
- просматривать собственные заказы;
- изменять собственные данные профиля.

Основные права:
```
├── User
│ ├── read
│ └── update
│
├── Shop
│ └── read_all
│
├── Product
│ └── read_all
│
└── Order
	├── read
	├── create
	├── update
	└── delete
```
При этом `read` для `Order` означает доступ только к заказам текущего пользователя.

### 6. Роль Seller

`Seller` — роль пользователя, которая предоставляет возможность управлять собственными магазинами, товарами и настройками доставки.

В текущей реализации роль `Seller` **не назначается автоматически при создании магазина**.

Для получения роли пользователь должен подать заявку, после чего заявка рассматривается администратором.

#### 6.1. Получение роли Seller

Авторизованный пользователь подает заявку на получение роли `Seller` через endpoint:

```
POST /api/seller-requests/
```

При создании заявки сервер проверяет:

1. является ли пользователь уже продавцом;
    
2. существует ли у пользователя активная заявка со статусом `PENDING`;
    
3. если пользователь уже является продавцом, создание заявки запрещается;
    
4. если активная заявка уже существует, создание новой заявки запрещается;
    
5. если предыдущая заявка была отклонена, пользователь может подать новую заявку.
    

При успешном создании заявке устанавливается статус:

```
PENDING
```

Упрощенно процесс выглядит следующим образом:

```
User
  │
  │ POST /api/seller-requests/
  ▼
Создание SellerRequest
  │
  └── status = PENDING
           │
           ▼
     Рассмотрение заявки
        ┌────┴────┐
        ▼         ▼
    APPROVED   REJECTED
        │         │
        ▼         ▼
     Seller    повторная
      role      заявка
```

#### 6.2. Одобрение заявки

Администратор может одобрить заявку через endpoint:

```
POST /api/seller-requests/{id}/approve/
```

При одобрении сервер:

1. проверяет, что заявка находится в статусе `PENDING`;
    
2. получает роль `Seller`;
    
3. создает связь пользователя с ролью `Seller`;
    
4. изменяет статус заявки на `APPROVED`;
    
5. очищает причину отказа, если она была установлена ранее.
    

Операция выполняется атомарно с использованием `transaction.atomic`.

Упрощенная схема:

```
SellerRequest
  │
  │ status = PENDING
  ▼
POST /seller-requests/{id}/approve/
  │
  ├── получение роли Seller
  │
  ├── UserRole(user, Seller)
  │
  └── status = APPROVED
```

После успешного одобрения пользователь получает роль:

```
User + Seller
```

#### 6.3. Отклонение заявки

Администратор может отклонить заявку через endpoint:

```
POST /api/seller-requests/{id}/reject/
```

При отклонении необходимо передать причину:

```
{
    "reason": "Необходимо предоставить дополнительные данные о магазине."
}
```

Сервер проверяет:

1. что заявка находится в статусе `PENDING`;
    
2. что причина отказа указана;
    
3. что причина не является пустой строкой.
    

После этого:

```
status = REJECTED
rejection_reason = указанная причина
```

После отклонения пользователь может повторно подать заявку.

Упрощенная схема:

```
SellerRequest
  │
  │ status = PENDING
  ▼
POST /seller-requests/{id}/reject/
  │
  ├── проверка причины
  │
  ├── сохранение причины отказа
  │
  └── status = REJECTED
           │
           ▼
      повторная заявка
```

#### 6.4. Жизненный цикл заявки Seller

Заявка может находиться в одном из трех состояний:

|Статус|Значение|
|---|---|
|`PENDING`|Заявка находится на рассмотрении|
|`APPROVED`|Заявка одобрена, пользователь получил роль `Seller`|
|`REJECTED`|Заявка отклонена с указанием причины|

Переходы между состояниями:

```
             ┌──────────────┐
             │   PENDING    │
             └──────┬───────┘
                    │
          ┌─────────┴─────────┐
          ▼                   ▼
   ┌──────────────┐    ┌──────────────┐
   │   APPROVED   │    │   REJECTED   │
   └──────────────┘    └──────┬───────┘
                              │
                              │ новая заявка
                              ▼
                         ┌──────────────┐
                         │   PENDING    │
                         └──────────────┘
```

Одобренная или отклоненная заявка не может быть повторно одобрена или отклонена. Такие операции разрешены только для заявок со статусом `PENDING`.

#### 6.5. Создание магазина Seller

После получения роли `Seller` пользователь может создавать собственные магазины:

```
POST /api/shops/
```

При создании магазина сервер:

1. определяет текущего пользователя;
    
2. создает объект `Shop`;
    
3. устанавливает текущего пользователя в качестве владельца магазина через поле `owner`;
    
4. выполняет инициализацию доставки через `DeliveryFactory.initialize(shop)`.
    

Упрощенно процесс выглядит следующим образом:

```
User + Seller
      │
      │ POST /api/shops/
      ▼
  Создание Shop
      │
      ├── owner = текущий пользователь
      │
      └── DeliveryFactory.initialize(shop)
```

В отличие от предыдущей реализации, создание магазина **не отвечает за назначение роли `Seller`**, поскольку пользователь уже должен получить эту роль через механизм заявки.

#### 6.6. Управление магазинами

Seller может работать только со своими магазинами.

Основные операции:

```
Seller
  │
  └── Shop
      ├── create
      ├── read
      ├── update
      └── delete
```

При работе с конкретным магазином дополнительно проверяется его принадлежность текущему пользователю.

Например, настройки доставки доступны только владельцу магазина:

```
Shop.owner == request.user
```

Таким образом, наличие роли `Seller` само по себе не предоставляет доступ ко всем магазинам системы.
Операции с настройками доставки явно ограничены магазинами, владельцем которых является текущий пользователь. Ограничение доступа к самим объектам `Shop` дополнительно определяется механизмом `RolePermission` и настройками доступа системы.

#### 6.7. Настройки доставки магазина

Для магазина предусмотрен отдельный объект `ShopDeliverySetting`, который хранит выбранные продавцом тарифы доставки.

Работа с настройками выполняется через:

```
GET    /api/shops/{shop_id}/delivery-settings/
POST   /api/shops/{shop_id}/delivery-settings/
DELETE /api/shops/{shop_id}/delivery-settings/
```

При сохранении передается список кодов тарифов:

```
{
    "tariffs": [1, 2, 5, 10]
}
```

Перед сохранением сервер получает актуальные тарифы из Redis и проверяет переданные коды.

Если передан код тарифа, которого нет среди актуальных тарифов, операция завершается ошибкой.

После успешной проверки текущие настройки магазина полностью заменяются новым набором тарифов.

История выбора тарифов не сохраняется.

Упрощенная схема:

```
Seller
  │
  │ выбирает тарифы
  ▼
ShopDeliverySettingService
  │
  ▼
Redis
  │
  │ актуальные тарифы
  ▼
проверка tariff_codes
  │
  ├── недоступный тариф → ошибка
  │
  └── все тарифы доступны
              │
              ▼
      сохранение настроек
```

#### 6.8. Удаление магазина и роль Seller

При удалении магазина выполняются следующие действия:

1. вызывается `DeliveryFactory.cleanup(shop)`;
    
2. удаляется объект `Shop`;
    
3. проверяется, остались ли у пользователя другие магазины;
    
4. если пользователь больше не владеет ни одним магазином, роль `Seller` удаляется.
    

Упрощенно:

```
Seller
  │
  │ удаление Shop
  ▼
DeliveryFactory.cleanup()
  │
  ▼
Shop.delete()
  │
  ▼
Проверка оставшихся магазинов
  │
  ├── есть магазины → Seller сохраняется
  │
  └── магазинов нет → Seller удаляется
```

Таким образом, роль `Seller` сохраняется до тех пор, пока пользователь владеет хотя бы одним магазином. При удалении магазина вызывается: 
```python
SellerService.remove_role_if_no_shops(owner)
```

и сервис проверяет:
```python
Shop.objects.filter(owner=user).exists()
```

`SellerService` содержит операции назначения и удаления роли `Seller`, однако назначение роли при создании магазина в текущей реализации не выполняется. Роль назначается через `SellerRequestService.approve()`.

SellerRequest является отдельной сущностью, которая связывает пользователя
с процессом получения роли Seller.

Заявка не является ролью и не предоставляет пользователю права Seller.
Права Seller появляются только после успешного выполнения операции approve,
которая создает связь UserRole между пользователем и ролью Seller.
Роль Seller появляется только после одобрения заявки:
```
SellerRequest(PENDING) 
│ 
│ 
approve 
▼ 
UserRole(user, Seller) 
│ 
▼ 
User + Seller
```
SellerService содержит операции назначения и удаления роли Seller.
В текущем сценарии получения роли назначение выполняется
через SellerRequestService.approve(), а не автоматически
при создании магазина.

При удалении магазина SellerService.remove_role_if_no_shops()
проверяет наличие других магазинов пользователя и удаляет роль,
если пользователь больше не владеет ни одним магазином.
#### 6.9. Итоговая модель работы Seller

Полный жизненный цикл пользователя в текущей реализации:

```
┌──────────────┐
│     User     │
└──────┬───────┘
       │
       │ POST /seller-requests/
       ▼
┌──────────────┐
│    PENDING   │
└──────┬───────┘
       │
       │ approve
       ▼
┌──────────────┐
│ User+Seller  │
└──────┬───────┘
       │
       │ POST /shops/
       ▼
┌──────────────┐
│     Shop     │
│ owner = User │
└──────┬───────┘
       │
       ├── Product
       │
       ├── UniqueProduct
       │
       └── Delivery Settings
              │
              ▼
       выбранные тарифы
```

Таким образом, `Seller` является дополнительной ролью пользователя и не заменяет роль `User`.

Получение роли выполняется через систему заявок, а создание магазина, управление товарами и настройками доставки выполняются уже после получения роли `Seller`.
#### 7. Пользователь с несколькими ролями

Один пользователь может иметь несколько ролей одновременно.

Например:
```
Пользователь
   │
   ├── User
   └── Seller
```
В этом случае роли не заменяют друг друга.
Права пользователя определяются совокупностью его ролей.

Например, такой пользователь:
Как User может:
- создавать собственные заказы;
- просматривать собственные заказы;
- просматривать товары;
- просматривать магазины;
- редактировать свой профиль.

Как Seller может:
- управлять своим магазином;
- управлять товарами своего магазина;
- работать с заказами своего магазина;
- управлять настройками доставки.

Таким образом, создание магазина не превращает пользователя из `User` в `Seller`, а **добавляет ему дополнительную роль**.

#### 8. Роль Admin

`Admin` имеет полный доступ к основным объектам системы.

Администратор может:
- просматривать пользователей;
- изменять пользователей;
- управлять магазинами;
- управлять товарами;
- управлять заказами;
- управлять ролями;
- управлять связями пользователей и ролей;
- управлять бизнес-элементами;
- управлять правилами доступа.

Для администратора используется специальный признак:

```
user.is_superuser
```

Если пользователь является суперпользователем, ограничения на получение объектов снимаются.

#### 9. Роль Guest

`Guest` представляет неавторизованного пользователя.

Guest не проходит JWT-аутентификацию и имеет доступ только к публичной информации.

Основные доступные объекты:
```
Guest
  ├── Product
  │ └── read_all
  │
  └── Shop
  └── read_all

```

Guest не может:
- создавать заказы;
- изменять профиль;
- создавать магазин;
- изменять товары;
- управлять магазином;
- управлять доставкой.

#### 10. Проверка доступа
Для защищенных endpoint используется два основных уровня проверки:
```
JWT
 │
 ▼
IsCustomAuthenticated
 │
 │ определяет текущего пользователя
 ▼
RolePermission
 │
 │ определяет доступные действия
 ▼
ViewSet
 │
 │ определяет доступные объекты
 ▼
Ответ API
```

`IsCustomAuthenticated` отвечает за проверку JWT-токена и определение текущего пользователя. Если токен отсутствует, недействителен или сессия неактивна, пользователь не получает доступ к защищенному endpoint.

`RolePermission` проверяет наличие необходимой роли и соответствующего права для бизнес-элемента.
Например:

```
Seller + Product + create
```

означает, что пользователь с ролью Seller имеет право создавать товары.

Однако наличие права `create` само по себе не означает возможность работать с любым объектом системы. Ограничение области объектов выполняется дополнительно на уровне ViewSet/queryset.

#### 11. Область действия права

В системе необходимо разделять два понятия:
**право на действие** и **область доступных объектов**.
Например:

```
Seller → Order → read
```
означает, что Seller имеет право просматривать заказы.

Но это не означает:
```
Seller → все Order
```

Seller должен видеть только заказы, относящиеся к его магазинам.
А пользователь с ролью `User` должен видеть только собственные заказы.
Поэтому для `Order` используется дополнительная фильтрация queryset.
В результате для пользователя с двумя ролями:
```
User + Seller
```

итоговый набор заказов должен формироваться как:

```
собственные заказы пользователя
+
заказы магазинов пользователя
```

То есть:
```
┌─────────────────────────────┐
│      Все заказы системы     │
└──────────────┬──────────────┘
               │
       ┌───────┴────────┐
       ▼                ▼
  Заказы User       Заказы Seller
  owner = user      shop.owner = user
        │                │
        └───────┬────────┘
           доступные заказы
```
При этом пользователь не получает доступ к заказам других пользователей или других продавцов.

#### 12. Жизненный цикл ролей

Основной сценарий работы пользователя:
```
User
 │
 │ регистрация
 ▼
User
 │
 │ login
 ▼
JWT + Session
 │
 │ POST /seller-requests/
 ▼
SellerRequest(PENDING)
 │
 │ approve
 ▼
User + Seller
 │
 │ POST /shops/
 ▼
Shop
```
При удалении магазина роль `Seller` не удаляется автоматически, если пользователь владеет другими магазинами.
Если пользователь больше не владеет ни одним магазином, то роль `Seller` удаляется автоматически. 

#### 13. Итоговая модель
Модель ролей:
```
┌──────────────┐
│    Guest     │
│  public data │
└──────────────┘
┌──────────────┐
│      User    │
│   покупатель │
└──────┬───────┘
       │
создает магазин
       │
       ▼
┌──────────────┐
│    Seller    │
│    владелец  │
│    магазина  │
└──────────────┘
┌──────────────┐
│     Admin    │
│     полный   │
│     доступ   │
└──────────────┘
```


## Модуль Product

### 1. Назначение модуля

Модуль `product` отвечает за управление товарами маркетплейса.

Основная функциональность модуля:
- создание и управление карточками товаров;
- привязка товара к магазину продавца;
- создание вариантов конкретного товара;
- хранение характеристик вариантов товара;
- хранение цены и артикула;
- управление остатками товара на складе;
- проверка принадлежности товара магазину текущего продавца;
- резервирование товара при оформлении заказа.

В модуле используется разделение между **карточкой товара** и **конкретной продаваемой единицей товара**.

Основная структура:
```
product/
├── models.py
├── serializers.py
├── services.py
├── views.py
└── urls.py
```

Общая архитектура:
```
HTTP-запрос
     │
     ▼
   ViewSet
     │
     ├── Serializer
     │
     └── Service
            │
            └── StockService
                    │
                    ▼
                UniqueProduct
```

### 2. Модель Product

`Product` представляет **карточку товара**.

Карточка описывает сам товар как объект каталога, но не является конкретной физической единицей товара.

Например, магазин может продавать футболку:
```
Product
└── name = "Футболка"
```

А конкретные варианты этой футболки могут отличаться размером и цветом:
```
UniqueProduct
├── красный / S
├── красный / M
├── синий / S
└── синий / M
```

Структура модели:
```
Product
├── id
├── name
└── shop
```

### Поля модели

|Поле|Тип|Назначение|
|---|---|---|
|`id`|AutoField|Уникальный идентификатор карточки товара|
|`name`|CharField|Название товара|
|`shop`|ForeignKey|Магазин, которому принадлежит товар|

Связь реализована следующим образом:

```
Shop
  │
  │ 1
  │
  └─────────── *
              │
           Product
```

Один магазин может содержать множество товаров.

При удалении магазина связанные товары удаляются автоматически:

```
shop = models.ForeignKey(
    Shop,
    on_delete=models.CASCADE,
    related_name="products",
)
```

Таким образом:

```
Shop
 │
 ├── Product 1
 ├── Product 2
 └── Product 3
```

при удалении `Shop` удаляются и связанные `Product`.
### 3. Модель UniqueProduct

`UniqueProduct` представляет **конкретный вариант товара**, который непосредственно продаётся на маркетплейсе.

Вариант может отличаться от других вариантов:

- артикулом;
- ценой;
- цветом;
- размером;
- габаритами;
- весом;
- количеством доступного товара на складе.

Структура:

```
UniqueProduct
├── id
├── product
├── ware_key
├── price
├── color
├── size
├── height
├── length
├── width
├── weight
└── stock
```

Связь:

```
Shop
  │
  └── Product
        │
        ├── UniqueProduct
        ├── UniqueProduct
        └── UniqueProduct
```

Таким образом, один `Product` может иметь несколько вариантов.

### 3.1. Поля UniqueProduct

|Поле|Тип|Назначение|
|---|---|---|
|`id`|AutoField|Уникальный идентификатор варианта|
|`product`|ForeignKey|Карточка товара|
|`ware_key`|CharField|Уникальный артикул варианта|
|`price`|DecimalField|Цена|
|`color`|CharField|Цвет|
|`size`|CharField|Размер|
|`height`|PositiveIntegerField|Высота в сантиметрах|
|`length`|PositiveIntegerField|Длина в сантиметрах|
|`width`|PositiveIntegerField|Ширина в сантиметрах|
|`weight`|PositiveIntegerField|Вес в граммах|
|`stock`|PositiveIntegerField|Текущий остаток на складе|

Поле `ware_key` является уникальным:

```
ware_key = models.CharField(
    max_length=100,
    unique=True,
)
```

Поэтому два разных варианта товара не могут иметь одинаковый артикул.


### 4. Разделение Product и UniqueProduct

В системе специально разделены два уровня товара:

```
Product
   │
   │ карточка товара
   ▼
UniqueProduct
   │
   ├── артикул
   ├── цена
   ├── цвет
   ├── размер
   ├── габариты
   ├── вес
   └── остаток
```

Например:

```
Product:
"Кроссовки Nike"

UniqueProduct:
├── SKU-001 / black / 40 / 7990 ₽
├── SKU-002 / black / 41 / 7990 ₽
├── SKU-003 / white / 40 / 8290 ₽
└── SKU-004 / white / 41 / 8290 ₽
```

Это позволяет одной карточке товара иметь несколько продаваемых вариантов.

### 5. Создание Product

Создание карточки товара выполняется через:

```
POST /api/products/
```

Пример запроса:

```
{
    "name": "Футболка",
    "shop": 5
}
```

При создании проверяется принадлежность магазина текущему пользователю.

Проверка выполняется в `ProductSerializer`:

```
if shop.owner != request.user:
    raise serializers.ValidationError(
        "Вы можете создавать товары только в своих магазинах."
    )
```

Таким образом, Seller не может создать товар в чужом магазине.

Общая схема:

```
User + Seller
      │
      │ POST /api/products/
      ▼
ProductSerializer
      │
      ▼
проверка Shop.owner
      │
 ┌────┴─────┐
 │          │
свой       чужой
Shop       Shop
 │          │
 ▼          ▼
создание   ошибка
Product
```


### 6. Изменение и удаление Product

Для `ProductViewSet` используется:

```
viewsets.ModelViewSet
```

Поэтому стандартно доступны CRUD-операции:

```
GET     /api/products/
POST    /api/products/
GET     /api/products/{id}/
PATCH   /api/products/{id}/
PUT     /api/products/{id}/
DELETE  /api/products/{id}/
```

Доступ к операциям дополнительно контролируется:

```
permission_classes = [
    IsCustomAuthenticated,
    RolePermission,
]
```

Бизнес-элемент:

```
business_element = "Product"
```

Поэтому сначала проверяется наличие соответствующего права у пользователя.

Дополнительно при создании товара проверяется принадлежность магазина текущему пользователю.

### 7. Создание UniqueProduct

Конкретный вариант товара создаётся через:

```
POST /api/unique-products/
```

Пример:

```
{
    "product": 10,
    "ware_key": "TSHIRT-RED-M",
    "price": "2499.00",
    "color": "Красный",
    "size": "M",
    "height": 5,
    "length": 30,
    "width": 25,
    "weight": 300
}
```

Поле `stock` клиент при создании напрямую не передаёт.

Оно имеет значение по умолчанию:

```
stock = 0
```

Кроме того, `stock` является `read_only` в `UniqueProductSerializer`.

После создания варианта:

```
stock = 0
```

Для добавления товара на склад используются отдельные API-операции.

### 8. Проверка принадлежности UniqueProduct

При создании или изменении `UniqueProduct` сервер проверяет не только сам объект `Product`, но и магазин, которому он принадлежит.

Проверка:

```
if product.shop.owner != request.user:
    raise serializers.ValidationError(
        "Вы можете добавлять варианты только в свои товары."
    )
```

Иерархия проверки:

```
UniqueProduct
      │
      ▼
   Product
      │
      ▼
    Shop
      │
      ▼
Shop.owner
      │
      ▼
request.user
```

Таким образом:

```
Seller
  │
  └── Shop
       │
       └── Product
            │
            └── UniqueProduct
```

Продавец может работать только с вариантами товаров своих магазинов.

### 9. API UniqueProduct

Для вариантов товара используется:

```
UniqueProductViewSet
```

Доступны стандартные CRUD-операции:

```
GET     /api/unique-products/
POST    /api/unique-products/
GET     /api/unique-products/{id}/
PUT     /api/unique-products/{id}/
PATCH   /api/unique-products/{id}/
DELETE  /api/unique-products/{id}/
```

Права проверяются через:

```
permission_classes = [
    IsCustomAuthenticated,
    RolePermission,
]
```

Бизнес-элемент:

```
business_element = "UniqueProduct"
```

### 10. Изменение UniqueProduct

Для изменения варианта товара доступны:

```
PUT /api/unique-products/{id}/
```

и:

```
PATCH /api/unique-products/{id}/
```

При этом поле:

```
stock
```

не может изменяться через стандартный `PUT` или `PATCH`.

Это сделано намеренно.

Изменение складского остатка выполняется через отдельные операции:

```
POST /api/unique-products/{id}/stock/increase/
POST /api/unique-products/{id}/stock/decrease/
```

Таким образом, изменение описания товара отделено от изменения его складского остатка.

### 11. Удаление UniqueProduct

Конкретный вариант товара удаляется через:

```
DELETE /api/unique-products/{id}/
```

После удаления:

```
UniqueProduct
      │
      ▼
DELETE
```

Удаляется только конкретный вариант.

Остальные варианты того же `Product` продолжают существовать.

Например:

```
Product: Футболка
│
├── UniqueProduct RED-S
├── UniqueProduct RED-M  ← удаляем
└── UniqueProduct BLUE-M
```

После удаления:

```
Product: Футболка
│
├── UniqueProduct RED-S
└── UniqueProduct BLUE-M
```

### 12. Управление остатками

Для управления количеством товара используется отдельный сервис:

```
StockService
```

Он отвечает за операции над полем:

```
UniqueProduct.stock
```

Основные методы:

```
get_stock()
increase()
decrease()
has_stock()
reserve()
```

Общая схема:

```
UniqueProduct
      │
      ▼
 StockService
      │
 ├── get_stock()
 ├── increase()
 ├── decrease()
 ├── has_stock()
 └── reserve()
```

### 13. Получение остатка

Метод:

```
get_stock(unique_product)
```

возвращает текущее количество товара:

```
return unique_product.stock
```

Например:

```
UniqueProduct
stock = 15
       │
       ▼
get_stock()
       │
       ▼
15
```

### 14. Пополнение склада

Для увеличения остатка используется endpoint:

```
POST /api/unique-products/{id}/stock/increase/
```

Пример:

```
{
    "amount": 10
}
```

Если до операции:

```
stock = 15
```

после операции:

```
stock = 25
```

Алгоритм:

```
POST
 │
 ▼
получение UniqueProduct
 │
 ▼
получение amount
 │
 ▼
проверка amount > 0
 │
 ▼
StockService.increase()
 │
 ▼
stock = stock + amount
 │
 ▼
HTTP 200
```

Пример ответа:

```
{
    "id": 1,
    "stock": 25
}
```

Количество должно быть положительным целым числом.

Недопустимо:

```
{
    "amount": 0
}
```

или:

```
{
    "amount": -5
}
```

В таком случае возвращается ошибка валидации.

Операция выполняется внутри:

```
@transaction.atomic
```


### 15. Списание товара со склада

Для уменьшения остатка используется:

```
POST /api/unique-products/{id}/stock/decrease/
```

Пример:

```
{
    "amount": 5
}
```

Если:

```
stock = 15
```

после операции:

```
stock = 10
```

Списание используется, например, при:

- браке;
- порче;
- утилизации;
- потере товара;
- других причинах уменьшения складского остатка.

Алгоритм:

```
POST
 │
 ▼
получение UniqueProduct
 │
 ▼
получение amount
 │
 ▼
amount > 0?
 │
 ▼
stock >= amount?
 │
 ├── Нет → ошибка
 │
 └── Да
       │
       ▼
stock = stock - amount
       │
       ▼
HTTP 200
```

Например, если:

```
stock = 3
amount = 5
```

операция запрещается:

```
Недостаточно товара на складе.
```

Остаток не может стать отрицательным.

### 16. Проверка наличия товара

Метод:

```
has_stock(
    unique_product,
    amount=1,
)
```

проверяет, достаточно ли товара на складе.

Условие:

```
unique_product.stock >= amount
```

Например:

```
stock = 10
```

Тогда:

```
has_stock(product, 5) → True
has_stock(product, 10) → True
has_stock(product, 11) → False
```

Метод используется бизнес-логикой для проверки возможности продажи необходимого количества товара.

### 17. Резервирование товара

Для оформления заказа предусмотрен метод:

```
StockService.reserve()
```

Он отличается от обычного `decrease()` тем, что выполняет блокировку строки базы данных.

Используется:

```
UniqueProduct.objects.select_for_update().get(
    id=unique_product_id
)
```

Это необходимо для предотвращения ситуации, когда несколько параллельных заказов одновременно пытаются приобрести один и тот же товар.

Общая схема:

```
Заказ 1 ─────┐
             │
Заказ 2 ─────┼──► UniqueProduct
             │
Заказ 3 ─────┘
                    │
                    ▼
              select_for_update()
                    │
                    ▼
             блокировка строки
                    │
                    ▼
             проверка stock
                    │
                    ▼
             уменьшение stock
```

Алгоритм:

```
reserve(unique_product_id, amount)
        │
        ▼
проверка amount > 0
        │
        ▼
select_for_update()
        │
        ▼
получение UniqueProduct
        │
        ▼
stock >= amount?
   /           \
 Нет            Да
 │               │
 ▼               ▼
ошибка      stock -= amount
                │
                ▼
              save()
```

Если товара недостаточно, резервирование завершается ошибкой:

```
Недостаточно товара на складе.
```

Операция выполняется атомарно:

```
@transaction.atomic
```

Таким образом, уменьшение остатка и проверка его достаточности выполняются в рамках одной транзакции.

### 18. Разделение операций с товаром и складом

В системе намеренно разделены две группы операций.

### Изменение данных товара

```
PUT /api/unique-products/{id}/
PATCH /api/unique-products/{id}/
```

Изменяются:

```
ware_key
price
color
size
height
length
width
weight
```

### Изменение складского остатка

```
POST /api/unique-products/{id}/stock/increase/
POST /api/unique-products/{id}/stock/decrease/
```

Изменяется только:

```
stock
```

Такое разделение не позволяет изменить остаток обычным редактированием карточки товара.

### 19. Взаимодействие Product и Seller

Основным пользователем модуля `product` является `Seller`.

Иерархия объектов:

```
User
 │
 └── Seller
      │
      └── Shop
           │
           └── Product
                │
                ├── UniqueProduct
                ├── UniqueProduct
                └── UniqueProduct
```

При этом роль `Seller` сама по себе не означает доступ ко всем товарам.

Область действия определяется принадлежностью объекта:

```
Product.shop.owner == request.user
```

или для варианта:

```
UniqueProduct.product.shop.owner == request.user
```

Таким образом:

```
Seller
  │
  ├── Shop A
  │    ├── Product 1
  │    └── Product 2
  │
  └── Shop B
       └── Product 3
```

Seller может управлять:

```
Product 1
Product 2
Product 3
```

если все три магазина принадлежат ему.

Товары других продавцов ему недоступны для изменения.

### 20. Проверка доступа

Для `ProductViewSet` и `UniqueProductViewSet` используется:

```
permission_classes = [
    IsCustomAuthenticated,
    RolePermission,
]
```

Проверка выполняется в несколько этапов:

```
HTTP-запрос
     │
     ▼
IsCustomAuthenticated
     │
     ▼
определение пользователя
     │
     ▼
RolePermission
     │
     ▼
проверка роли и permission
     │
     ▼
Serializer
     │
     ▼
проверка принадлежности Shop
     │
     ▼
операция с Product
```

Таким образом, существуют два разных уровня контроля:

```
1. Имеет ли пользователь необходимое право?
                +
2. Принадлежит ли товар его магазину?
```

## Архитектура корзины (модуль order)

`Cart` представляет текущую корзину авторизованного пользователя.

| id  | owner | created_at | updated_at | items_total |
| --- | ----- | ---------- | ---------- | ----------- |
| 23  | 2     | 09.08.2026 | 12.08.2026 | 48000.00    |
`items_total` — вычисляемое значение, представляющее общую стоимость всех товаров в корзине без стоимости доставки. Оно рассчитывается на основе количества `CartItem` и актуальной цены связанного `UniqueProduct`.

У одного пользователя существует не более одной корзины, что обеспечивается использованием `OneToOneField`
Корзина хранится в базе данных, поскольку должна сохраняться между пользовательскими сессиями и запросами:

- закрытием браузера;
- повторной авторизацией;
- использованием другого устройства;
- изменением количества товара;
- удалением товара;
- добавлением новых товаров;
- длительным периодом между добавлением товара и оформлением заказа.

Корзина создается **лениво** с помощью `get_or_create()` при первом обращении к ней или при первом добавлении товара.

Например:

```
User
 │
 └── Cart
      │
      ├── CartItem
      ├── CartItem
      └── CartItem
```


CartItem представляет одну позицию корзины — конкретный вариант товара (`UniqueProduct`) и выбранное пользователем количество. Один `UniqueProduct` может присутствовать в одной корзине только один раз. Это обеспечивается ограничением "UniqueConstraint". Поэтому при повторном добавлении того же варианта товара необходимо не создавать вторую строку, а изменять количество существующего `CartItem`.
В API цена каждой позиции возвращается в поле `price`:

| id  | cart | unique_product | amount | price    |
| --- | ---- | -------------- | ------ | -------- |
| 1   | 23   | 3              | 4      | 12000.00 |
| 2   | 23   | 5              | 2      | 8500.00  |
`price` не является отдельным полем модели `CartItem`. Это вычисляемое поле сериализатора, получаемое из `unique_product.price`.

`CartItem` не хранит собственную копию цены товара. Цена позиции определяется динамически через связанный `UniqueProduct`:
```
CartItem 
│ 
└── unique_product 
		│ 
		└── price
```

Поле `price` является `read_only` и вычисляется на основе   `UniqueProduct.price`.

Например:
```json
{
    "id": 1,
    "unique_product": 3,
    "amount": 4,
    "price": "12000.00"
}
```

Цена не фиксируется в момент добавления товара в корзину. Поэтому при  
изменении цены `UniqueProduct` значение `price` в корзине также изменится  
при следующем получении данных корзины.

Таким образом, корзина хранит:
- конкретный вариант товара;
- количество выбранных единиц;
- актуальную цену связанного `UniqueProduct`.

Историческая цена товара должна фиксироваться уже в сущностях заказа при  
оформлении покупки, а не в `CartItem`.

`Cart` предоставляет поле `items_total`, которое содержит общую стоимость всех товаров в корзине без стоимости доставки. Она рассчитывается как сумма:
```
items_total = CartItem.amount × UniqueProduct.price 
+ CartItem.amount × UniqueProduct.price 
+ ...
```

Например:

| Товар    | Количество | Цена     | Сумма    |
| -------- | ---------- | -------- | -------- |
| Nike Air | 4          | 12000.00 | 48000.00 |
| Adidas   | 2          | 8500.00  | 17000.00 |
| Итого    |            |          | 65000.00 |

В API:

```json
{
    "id": 23,
    "owner": 2,
    "items": [
        {
            "id": 1,
            "unique_product": 3,
            "amount": 4,
            "price": "12000.00"
        }
    ],
    "items_total": "48000.00"
}
```

`items_total` не включает стоимость доставки. Стоимость доставки  
рассчитывается отдельно на этапе оформления заказа, когда известны параметры  
доставки.

После успешного оформления заказа сама корзина не удаляется. Удаляются только ее позиции:

```
User
 │
 └── Cart
      │
      ├── CartItem
      ├── CartItem
      └── CartItem
```

становится:
```
Cart
 └── items = []
```

После заказа:
```
User
  │
  ├── Cart
  │    └── пустая
  │
  └── Order
       ├── OrderProduct
       └── OrderProduct
```
При следующей покупке пользователь продолжает использовать ту же корзину.

На этапе добавления в корзину проверяется, что товар существует и количество положительное.
На этапе работы с корзиной **товар не резервируется**. При добавлении CartItem  проверяется корректность данных, в частности:

- существует ли указанный `UniqueProduct`;
- является ли количество положительным.
При этом stock НЕ изменяет.
Это необходимо, поскольку корзина не является гарантией покупки. Другой пользователь может приобрести этот товар раньше, либо продавец может изменить остаток. Поэтому окончательная проверка наличия товара выполняется **непосредственно при оформлении заказа**.

CRUD корзины:

| Метод  | Endpoint              | Назначение               |
| ------ | --------------------- | ------------------------ |
| GET    | /api/cart/            | Получить текущую корзину |
| POST   | /api/cart/items/      | Добавить товар в корзину |
| PATCH  | /api/cart/items/{id}/ | Изменить количество      |
| DELETE | /api/cart/items/{id}/ | Удалить позицию          |
| DELETE | /api/cart/            | Очистить корзину         |

Структура API:

```
/api/cart/
│
├── GET
│     └── получить корзину
│
├── DELETE
│     └── очистить корзину
│
└── /items/
      │
      ├── POST
      │     └── добавить товар
      │
      └── /{id}/
            │
            ├── PATCH
            │     └── изменить количество
            │
            └── DELETE
                  └── удалить позицию
```


## Архитектура предварительного расчета доставки (модуль delivery)

Перед оформлением заказа покупатель должен выбрать тариф для каждой группы заказов, сгруппированной по магазину. Для этого он должен видеть список предлагаемых тарифов для каждого магазина, из которого заказываются товары.

Для определения транспортной компании товары (UniqueProduct) в корзине покупателя (Cart) группируются по принадлежности к магазину (Shop): UniqueProduct.product.shop.

Если для группы товаров транспортной компанией является СДЭК (Shop.carrier=1), то для 
расчета с помощью модуля API СДЕКа  "**Расчет по доступным тарифам и дополнительным услугам**" данные берутся из:
- магазина продавца - город отправления (Shop.location_from):
		- "from_location".
- данных покупателя - город назначения (User.location_to):
		- "to_location"
- товаров, добавленных в корзину (`Cart` → `CartItem` → `UniqueProduct`) - габариты:
		- "height";
		- "length";
		- "weight";
		- "width".
		- 
Полученный ответ от API СДЕКа валидируем:
	- все ли магазины имею в настройках тарифы;
	- все ли магазины попали в предварительный расчет;
	- все ли товары (`UniqueProduct`) попали в предварительный расчет;

и фильтруем по тарифам, указанным в настройках магазина:
```python
# Из настроек достаем тарифы
tariffs = [setting.tariff for setting in shop.delivery_settings.select_related('tariff')]
```
 
 и возвращаем JSON со структурой:
 ```json
 [
  {
    "shop_id": 1,
    "shop_name": "Магазин А",
    "list_unique_product": [unique_product_1, unique_product_2],
    "options": [
      {
        "tariff_code": 121,
        "tariff_name": "Экономичная посылка",
        "delivery_sum": 1470.0,
        "period_min": 0,
        "period_max": 1,
        "delivery_date_range": {
          "min": "2026-07-31",
          "max": "2026-08-01"
        },
        "total_sum": 1764.0
      },
      {
        "tariff_code": 59,
        "delivery_sum": 2790.0,
        ...
      }
    ]
  },
  ...
]
 ```

Добавляем в фабрику метод для получения адаптера:
```python
class DeliveryFactory:
	# Реестр адаптеров  
	_adapters = {  
	    DeliveryType.CDEK: CDEKAdapter,    # Сюда будете добавлять новые: DeliveryType.POST: RussianPostAdapter,  
	}
	@classmethod  
	def get_adapter(cls, shop: Shop):  
	    """  
 	    Возвращает экземпляр нужного адаптера по типу перевозчика.                   DeliveryFactory.get_adapter(DeliveryType.CDEK) -> CDEKAdapter()    
 	    """    
 	    adapter_class = cls._adapters.get(shop.carrier)  
	  
	    if not adapter_class:  
	        raise NotImplementedError(f"Адаптер для carrier={shop.carrier} не реализован")  
	  
	    return adapter_class()
```
Добавляем метод в class DeliveryFacade:
```python
class DeliveryFacade:  
    def __init__(self, adapter):  
        self.adapter = adapter  
        
    def pre_calculate_delivery(self, data):  
        """Предварительный расчет доставки (до оформления Order)"""  
        pass
```

Метод class DeliveryFacade.pre_calculate_delivery() должен:

1. Получить корзину пользователя.
    
2. Сгруппировать товары по магазинам.
    
3. Для каждого магазина получить адаптер через фабрику.
    
4. Вызвать метод адаптера `pre_calculate_delivery` (который возвращает сырой ответ от API, возможно уже валидированный Pydantic).
    
5. Передать ответ адаптера в сервисный слой (например, `DeliveryOptionsService`), который:
    
    - Проверит, что магазин имеет настройки тарифов.
        
    - Отфильтрует ответ по разрешённым тарифам из `ShopDeliverySetting`.
        
    - Преобразует ответ в нужную структуру.
        
6. Собрать результаты по всем магазинам в единый список и вернуть.
    

Таким образом, фасад не занимается бизнес-логикой обработки ответа, а только координирует вызовы.



## Архитектура заказа (модуль delivery)

Пользователь нажимает "**Оформить заказ**":
```
1. Получить Cart
       ↓
2. Получить CartItem
       ↓
3. Заблокировать UniqueProduct
       ↓
4. Проверить stock
       ↓
5. Зарезервировать stock
       ↓
6. Создать Order
       ↓
7. Создать OrderProduct
       ↓
8. Создать OrderDelivery
       ↓
9. CDEK registration
       ↓
10. изменение статуса OrderDelivery
       ↓
11. очистка Cart
```


Общий процесс покупки:
```
User
 │
 ▼
Cart
 │
 │ добавление товаров
 │
 ▼
Расчет стоимости товаров
 │
 │ выбор способа доставки
 ▼
Предварительный расчет доставки
 │
 ▼
Проверка актуальности товаров
 │
 ├── товар существует
 ├── актуальная цена
 └── достаточно товара на складе
 │
 ▼
Резервирование товара
 │
 ▼
Расчет итоговой стоимости
 │
 ├── стоимость товаров
 └── стоимость доставки
 │
 ▼
Создание Order
 │
 ├── OrderProduct
 │
 └── OrderDelivery
        │
        └── регистрация отправления в CDEK
 │
 ▼
Оплата заказа
```


Shop

| id  | name             | legal_info                     | owner | location_from | carrier |
| --- | ---------------- | ------------------------------ | ----- | ------------- | ------- |
| 1   | Классный магазин | г. Самара, ул. Мичурина, дом 2 | 9     | Самара        | 1       |


Product

| id  | name               | shop |
| --- | ------------------ | ---- |
| 1   | Кроссовки Nike Air | 1    |
| 2   | Джинсы Levi's 501  | 1    |

UniqueProduct

| id  | product_id | ware_key  | color  | size | weight | price | Stock | width | length | weight |
| --- | ---------- | --------- | ------ | ---- | ------ | ----- | ----- | ----- | ------ | ------ |
| 1   | 1          | 501245789 | Белый  | 42   | 800    | 12000 | 5     | 8     | 42     | 8      |
| 2   | 1          | 689545123 | Черный | 42   | 800    | 12000 | 2     | 8     | 42     | 8      |
| 3   | 1          | 568945127 | Белый  | 43   | 850    | 13000 | 0     | 8     | 43     | 9      |
| 4   | 2          | 689547812 | Синий  | XS   | 550    | 9500  | 3     | 20    | 15     | 7      |

Order

| id  | owner | created_at |
| --- | ----- | ---------- |
| 7   | 4     | 07.07.2026 |

OrderProduct

| id  | order_id | unique_product_id | product_name                   | amount | price | order_delivery |
| --- | -------- | ----------------- | ------------------------------ | ------ | ----- | -------------- |
| 1   | 7        | 1                 | Кроссовки Nike Air, белый, 42  | 1      | 12000 | 1              |
| 2   | 7        | 2                 | Кроссовки Nike Air, черный, 42 | 1      | 12000 | 1              |
| 3   | 7        | 4                 | Джинсы Levi's 501, синий, XS   | 2      | 9500  | 1              |
Здесь `price` — **цена на момент оформления заказа**.
Здесь 'product_name' - **название товара на момент оформления заказа**
Т.е. даже если продавец удалит товар или переименует его, заказ останется неизменным и будет отражать состояние каталога на момент покупки.


## Архитектура связи доставки с товарами заказа

```
Покупатель
    │
    ▼
Order (одна покупка / один checkout)
    │
    ├──────────────┐
    ▼              ▼
OrderDelivery   OrderDelivery
(магазин A)     (магазин B)
    │               │
    ▼               ▼
OrderProduct(1)    OrderProduct(2)
OrderProduct(3)
```

```
order_delivery.items.all()      # товары этой отправки

order.deliveries.all()    # все отправки заказа

order.items.all()          # все товары заказа
```

# Структура модуля Delivery

```
delivery/
│
├── api/
│   ├── client.py              # Singleton + базовое подключение к API СДЭК
│   ├── facade.py              # Единая точка входа для всего модуля доставки
│   ├── exceptions.py          # Исключения и обработка ошибок
│   ├── constants.py
│   └── adapters/          # Adapter для разных служб доставки
│        ├── __init__.py 
│        ├── base.py
│        └── cdek.py
│
│── routes/ # Модуль с константами маршрутов внешнего API служб доставки
│   ├── __init__.py 
│   └── routes_cdek.py   # endpoints: ORDER = "/orders", ORDER_UUID = "/orders/{uuid}"
│
│── decorators/ # Логирование, повторные запросы, проверка токена
│      ├── __init__.py 
│      └── decorators_cdek.py
│
├── strategies.py     # Strategy выбора вида доставки у службы доставки
├── iterators.py     # Генераторы и ленивые итераторы: iter_delivery_points(),          │                     # iter_tariffs(), iter_packages(), iter_order_items()
├── utils.py
├── admin.py
├── enums.py       # DeliveryStatus, WebhookType, OrderType, ServiceType, TariffMode
├── mappers.py     # будет преобразовывать: Django Model -> Pydantic -> JSON CDEK
├── models.py
├── managers.py # bulk_update_or_create
├── celery.py  # настройки celery
├── views.py
├── urls.py
└── serializers.py
│
├── schemas/ # pydantic схемы
│      ├── __init__.py 
│      ├── common.py
│      ├── tariffs.py
│
├── state/
│     ├── __init__.py  
│     ├── machine.py  
│     ├── states.py  # состояние доставки
│     └── transitions.py
│
├── factories/   
│     ├── __init__.py 
│     └── delivery.py # вернет нужный Adapter:DeliveryFactory.create(DeliveryType.CDEK)
│ 
├── tasks/          # Задачи Celery
│   ├── __init__.py
│   ├── orders.py
│   ├── offices.py
│   ├── tariffs.py
│   └── webhooks.py
│
├──  cache/
│   ├── offices.py     # get_offices(), update_offices()
│   └── tariffs.py    # get_tariffs(),   update_tariffs()
│
├── schemas/
│   ├── tariffs.py             # Pydantic Request/Response
│   ├── offices.py
│   ├── orders.py
│   ├── webhooks.py
│   └── common.py
│
├── services/
│   ├── tariffs.py # бизнес-логика маркетплейса работ с тарифами
│   ├── offices.py
│   ├── orders.py
│   ├── webhooks.py
│   └── tracking.py # status
│
├── observers/           # Observer (вебхуки)
│   ├── __init__.py
│   ├── webhook.py
│   └── delivery_status.py
│
├── tests/          # Юнит тесты
│   ├── __init__.py
│   ├── test_cdek_client.py
│   ├── test_client.py
│   ├── test_tariffs.py
│   ├── test_views.py
│   ├── test_tasks.py


```


# Архитектура обработки исключений

### Архитектура создания заказа
```
OrderService # знает, что нужно оформить доставку
      │  
      ▼
DeliveryFacade # единая точка входа для всех служб доставки
      |
      ▼
DeliveryFactory # выбирает нужный адаптер (`CDEKAdapter`, `BoxberryAdapter`, ...).
      |
      ▼
CDEKAdapter # Содержит всю реализацию API СДЭК
      |
      ▼
CDEK API
```


### Архитектура обработки ответа СДЭК
```
                 HTTP запрос
                     │
                     ▼
                CDEK API
                     │
                     ▼
              HTTP Response
                     │
                     ▼
               CDEKAdapter
                     │
        ┌────────────┼─────────────┐
        │            │             │
        ▼            ▼             ▼
 DeliveryError   CDEKApiError   CDEKBusinessError
 (нет ответа)    (HTTP 4xx/5xx)   (HTTP 200, но
                                  бизнес-операция
                                  завершилась ошибкой)
                     │
                     ▼
                   result
```

**class DeliveryError** - базовое исключение модуля доставки, от него наследуются все остальные. Нет соединения с API (timeout, DNS, SSL, ConnectionError).
**class CDEKApiError** - ошибка транспортного уровня. API вернул HTTP 4xx/5xx.
**class CDEKBusinessError** - ошибка бизнес-логики: API вернул HTTP 200, но операция завершилась с `state=INVALID`.

# Архитектура подключения к API СДЕК по паттерну Singletone
## Назначение CDEKClient
`CDEKClient` является единым клиентом для взаимодействия с API СДЭК.  
  
Он отвечает за:  
- создание HTTP-сессии;  
- управление OAuth-аутентификацией;  
- хранение активного токена доступа;  
- автоматическое обновление токена после истечения срока действия;  
- выполнение запросов к API СДЭК.  
  
Использование Singleton позволяет использовать одно подключение во всем приложении и избежать повторного создания HTTP-сессий и лишних запросов получения токена.
## Схема работы Singleton  
  
Первый вызов клиента:

```
CDEKAdapter  
│  
▼  
CDEKClient()  
│  
▼  
Проверка существования экземпляра  
│  
▼  
Создание объекта CDEKClient  
│  
▼  
Создание requests.Session()  
│  
▼  
Получение OAuth-токена  
│  
▼  
Сохранение:  
- access_token  
- expires_at
```
Последующие вызовы:
```
CDEKAdapter  
│  
▼  
CDEKClient()  
│  
▼  
Возвращается существующий объект  
│  
▼  
Используется текущая Session  
│  
▼  
Проверяется срок действия токена
```

## Жизненный цикл OAuth-токена  
  
При выполнении любого запроса:
```
CDEKClient  
│  
▼  
Проверка access_token  
│  
├── токен существует и не истек  
│ │  
│ ▼  
│ выполнение запроса  
│  
└── токен отсутствует или истек  
│  
▼  
получение нового токена  
│  
▼  
выполнение запроса
```

Токен сохраняется в памяти клиента до момента истечения срока действия.  
  
Перед истечением срока действия добавляется запас времени (`30 секунд`), чтобы избежать ситуации, когда токен станет недействительным во время выполнения запроса.


### HTTP-сессия  
Для выполнения запросов используется:

**requests.Session()**
Сессия позволяет:  
- переиспользовать TCP-соединение;  
- хранить общие параметры запросов;  
- уменьшить количество повторных подключений к API СДЭК.

### Обработка ошибок

**CDEKClient** отвечает только за транспортный уровень:
- подключение;  
- авторизацию;  
- HTTP-запросы;  
- обработку HTTP-ошибок.  
  
Т.е. обработка ошибок осуществляется только классом CDEKApiError:  Бизнес-ошибки API СДЭК обрабатываются на уровне адаптера (`CDEKAdapter`).


# Архитектура создания заказа в системе СДЭК
```
Marketplace
     |
     |
DeliveryFacade
     |
     |
DeliveryAdapter (интерфейс)
     |
     |
CDEKAdapter
     |
     |
CDEKClient
     |
     |
CDEK API
```

- `CDEKClient` — **не знает бизнес-логику доставки**, он только HTTP-клиент:
    - OAuth;
    - токен;
    - GET/POST/DELETE;
    - обработка транспортных ошибок.
- `CDEKAdapter` — **адаптирует API СДЭК под интерфейс маркетплейса**:
    - формирует данные заказа;
    - вызывает клиент;
    - анализирует бизнес-ответы;
    - преобразует ответ СДЭК в внутренний формат.
- `DeliveryFacade` — **единая точка входа бизнес-логики**:
    - "создать доставку";
    - "рассчитать доставку";
    - "получить статус";
    - "отменить".

## Методы CDEKAdapter

### 1. Получение тарифов
_Модуль «Расчет стоимости доставки»: метод «Список доступных тарифов»_

Получение тарифов является административной операцией и не связано напрямую с оформлением заказа пользователем.

Справочник тарифов СДЭК хранится единым набором данных для всей платформы. Тарифы не привязываются к конкретному магазину, так как API СДЭК возвращает общий список доступных тарифов по договору.

Магазины используют общий справочник через локальные настройки `ShopDeliverySetting`, где продавец выбирает доступные тарифы для своего магазина.

Синхронизация тарифов выполняется периодической Celery-задачей `sync_cdek_tariffs`.

Перед обращением к API СДЭК выполняется проверка наличия магазинов, использующих данную службу доставки.

Если на платформе отсутствуют магазины с выбранным перевозчиком СДЭК, выполнение задачи завершается без обращения к внешнему API.
```
             Celery Beat
                  │
                  ▼
      sync_cdek_tariffs()
                  │
                  ▼
   Проверка магазинов с carrier=CDEK
                  │
      +-----------+-----------+
      │                       │
      ▼                       ▼
Нет магазинов CDEK       Есть магазины CDEK
      │                       │
      ▼                       ▼
```

Завершение задачи CDEKTariffService:
```
status="skipped" │  
▼  
CDEKAdapter  
│  
▼  
CDEKClient  
│  
▼  
CDEK API  
│  
▼  
JSON (dict)  
│  
▼  
AvailableTariffsResponseSchema  
(валидация Pydantic)  
│  
▼  
prepare_tariffs()  
│  
▼  
list[dict] для сохранения  
│  
+------------+------------+  
│ │  
▼ ▼  
bulk_update_or_create() update_cache()  
PostgreSQL Redis
```


После получения ответа от API СДЭК данные проходят валидацию через Pydantic-модель `AvailableTariffsResponseSchema`.

На этапе валидации проверяются:
- наличие обязательных полей;
- соответствие типов данных;
- корректность структуры ответа API.

После успешной проверки данные передаются в `CDEKTariffService`, где преобразуются в упрощенную структуру, необходимую для хранения в PostgreSQL и последующего использования через Redis.

Бизнес-логика синхронизации размещается в delivery/services/tariffs.py.
Celery-задача расположена в delivery/tasks/tariffs.py.

### Расписание синхронизации 
Запуск выполняется автоматически через Celery Beat один раз в сутки в 03:00. Настройка расписания:
```python
CELERY_BEAT_SCHEDULE = { 
"sync-cdek-tariffs": 
{ "task": 
"delivery.tasks.tariffs.sync_cdek_tariffs", 
"schedule": crontab(hour=3, minute=0), 
}, 
}
```

Также задача может быть вызвана вручную для принудительного обновления справочника тарифов.

#### Алгоритм синхронизации

1. Celery Beat запускает задачу `sync_cdek_tariffs`.
2. Выполняется проверка наличия магазинов с перевозчиком `DeliveryType.CDEK`.
3. Если магазины СДЭК отсутствуют, задача завершается без выполнения синхронизации.
4. Если существует хотя бы один магазин СДЭК:
    - запускается `CDEKTariffService.sync_cdek_tariffs()`;
    - сервис обращается к `CDEKAdapter`;
    - адаптер получает данные через `CDEKClient`;
    - выполняется запрос к API СДЭК.
5. Полученный JSON-ответ преобразуется в `AvailableTariffsResponseSchema`.
6. Метод `prepare_tariffs()` формирует генератор подготовленных данных:
    - перебирает тарифы;
    - перебирает режимы доставки (`delivery_modes`);
    - формирует только необходимые поля для хранения.
7. Новые и измененные тарифы сохраняются в PostgreSQL через:
```python
bulk_update_or_create()
```
8. Тарифы, которые отсутствуют в новом ответе API, помечаются как неактивные:
```python
is_active=False
```
9. После успешного обновления актуальный список активных тарифов сохраняется в Redis.
10. В журнал приложения записывается количество обработанных тарифов.

#### Использование Celery
```
               Celery Beat
                     │
                     ▼
             Redis (очередь)
                     │
                     ▼
              Celery Worker
                     │
                     ▼
        sync_cdek_tariffs()
                     │
                     ▼
      Проверка наличия магазинов CDEK
                     │
          +----------+----------+
          │                     │
          ▼                     ▼
      Skip task        CDEKTariffService
                              │
                              ▼
                         CDEKAdapter
                              │
                              ▼
                          CDEKClient
                              │
                              ▼
                           CDEK API
                              │
                +-------------+-------------+
                │                           │
                ▼                           ▼
           PostgreSQL                  Redis Cache
        (справочник тарифов)      (актуальные тарифы)
```

Планировщик реализован с использованием Celery.

В проекте используются три отдельных Docker-контейнера:

- `web` — Django-приложение;
- `celery` — обработчик асинхронных задач;
- `celery-beat` — планировщик периодических задач.

В качестве брокера сообщений и backend для хранения результатов используется Redis.

#### Особенность управления несколькими магазинами

Так как справочник тарифов СДЭК является общим ресурсом, система не создает отдельную Celery-задачу для каждого магазина.

При создании нового магазина:
```ShopViewSet
      │
      ▼
DeliveryFactory.initialize(shop)
      │
      ▼
initialize_cdek(shop)
      │
      ▼
Проверка существования магазинов CDEK
      │
      ▼
sync_cdek_tariffs.delay()
```

Если хотя бы один магазин уже использует СДЭК, повторный запуск синхронизации не требуется.

При удалении последнего магазина СДЭК выполняется очистка общего ресурса:

- удаляется Redis-кэш тарифов;
- удаляется справочник тарифов СДЭК из PostgreSQL;
- удаляются локальные настройки выбранных тарифов магазина.

Если остаются другие магазины СДЭК, удаляются только персональные настройки удаленного магазина.

#### Управление общими ресурсами доставки

Для справочников доставки используется принцип Shared Resource.

Данные, которые не зависят от конкретного магазина (например, список тарифов СДЭК), хранятся единым набором данных.

При этом применяется Lazy Initialization:

- если на платформе отсутствуют магазины с определенной службой доставки, синхронизация и хранение справочника не выполняются;
- при появлении первого магазина с данной службой запускается инициализация ресурса;
- при удалении последнего магазина ресурс очищается.

Такой подход позволяет избежать:
- создания одинаковых задач Celery для каждого магазина;
- лишних запросов к внешнему API;
- хранения неиспользуемых данных.
#### Использование Redis

Redis не является основным хранилищем данных, а используется как кэш.

Redis выполняет две функции:
- используется Celery в качестве брокера сообщений и хранения результатов задач;
- используется приложением как кэш списка активных тарифов, чтобы исключить повторные обращения к PostgreSQL при настройке способов доставки.

В кэше хранится сокращенная информация, необходимая магазинам при настройке способов доставки:

```json
{
    "tariff_code": 231,
    "tariff_name": "Экономичная посылка",
    "delivery_mode": 1,
    "delivery_mode_name": "дверь-дверь"
}
```
При открытии настроек магазина приложение сначала обращается к Redis. Если данные присутствуют в кэше, они возвращаются без обращения к базе данных, что уменьшает нагрузку на PostgreSQL и ускоряет работу интерфейса.

```
                 Celery Beat (1 раз в сутки)
                          |
                          v
            TariffService.sync_cdek_tariffs()
                          |
                          v
             CDEKAdapter.get_all_tariffs()
                          |
                          v
                     CDEK API
                          |
                          v
              Generator (yield по тарифам)
                          |
                          v
         Подготовка сокращенной структуры данных
                          |
                +---------+----------+
                |                    |
                v                    v
 bulk_update_or_create()      update_cache()
      PostgreSQL                  Redis
 (постоянное хранение)      (быстрый доступ
                              магазинам)
```

Такой подход позволяет разделить ответственность компонентов:
- **CDEK API** - источник актуальных тарифов;
- **PostgreSQL** - постоянное хранилище и единый источник данных;
- **Celery Beat** - автоматическая ежедневная синхронизация без участия пользователя;
- **CDEKClient** - выполняет HTTP-запросы и возвращает JSON;
- **CDEKAdapter** - преобразует JSON в типизированные Pydantic-модели;
- **AvailableTariffsResponseSchema** - валидирует структуру ответа API СДЭК;
- **CDEKTariffService** - реализует бизнес-логику синхронизации тарифов;
- **CDEKTariffManager** - массово создает и обновляет записи в базе данных;
- **Redis** - хранит кэш активных тарифов для быстрого доступа.

#### Преимущества реализации
- автоматическая ежедневная синхронизация без участия пользователя;
- валидация ответов API с помощью Pydantic;
- массовое обновление записей в PostgreSQL без лишних SQL-запросов;
- автоматическая деактивация устаревших тарифов;
- использование Redis для быстрого доступа к актуальным тарифам;
- асинхронное выполнение синхронизации без блокировки веб-приложения;
- разделение ответственности между `CDEKClient`, `CDEKAdapter`, `CDEKTariffService` и Celery-задачей.

#### Логика получения и сохранения тарифов CDEK

Для хранения доступных тарифов используется модель **`CDEKTariff`**, содержащая актуальные тарифы, полученные из API CDEK. Справочник тарифов ежедневно синхронизируется с API CDEK, сохраняется в PostgreSQL, после чего экспортируется в Redis (`cdek:tariffs`), который используется как кэш при работе личного кабинета продавца.

Для хранения выбранных продавцом тарифов используется модель **`ShopDeliverySetting`**, связывающая магазин с выбранными тарифами CDEK.

Логика работы реализована в сервисе **`ShopDeliverySettingService`** (`seller/services.py`).

При сохранении настроек магазина выполняются следующие действия:

1. Получение актуального списка тарифов из Redis (`cdek:tariffs`).
2. Проверка, что все выбранные продавцом тарифы присутствуют среди доступных.
3. При обнаружении недоступных или устаревших тарифов генерируется исключение `ValueError`, а сохранение отменяется.
4. Получение объектов `CDEKTariff` по выбранным кодам.
5. Удаление ранее сохраненных настроек магазина.
6. Массовое сохранение новых настроек (`bulk_create`) в таблицу `ShopDeliverySetting`.

Вся операция выполняется внутри атомарной транзакции (`transaction.atomic`), поэтому изменения либо сохраняются полностью, либо не сохраняются вовсе.

Схема работы сервиса:

```
Redis (cdek:tariffs)
        │
        ▼
получение актуальных тарифов
        │
        ▼
проверка выбранных кодов
        │
        ▼
получение объектов CDEKTariff
        │
        ▼
удаление старых настроек магазина
        │
        ▼
bulk_create новых настроек
```

#### Эндпоинты управления магазинами (`seller/views.py`)

##### 1. Создание магазина (POST /api/shops/)
При создании магазина:
1. Выполняется проверка входных данных через `ShopSerializer`.
2. Новый магазин автоматически привязывается к текущему пользователю (`owner=request.user`).
3. После сохранения магазина вызывается `DeliveryFactory.initialize(shop)`.
4. Фабрика определяет выбранную службу доставки (`carrier`) и запускает необходимую инициализацию.
5. Для СДЭК проверяется наличие других магазинов, использующих эту службу доставки.
6. Если магазин является первым пользователем СДЭК, запускается Celery-задача синхронизации общего справочника тарифов.

Схема работы:
```
POST  
│  
▼  
ShopViewSet.create()  
│  
▼  
ShopSerializer  
│  
▼  
serializer.save(owner=request.user)  
│  
▼  
Shop  
│  
▼  
DeliveryFactory.initialize(shop)  
│  
▼  
initialize_cdek(shop)  
│  
▼  
Проверка существования магазинов СДЭК  
│  
├── Уже есть магазины СДЭК  
│ │  
│ ▼  
│ ничего не выполняется  
│  
└── Первый магазин СДЭК  
│  
▼  
sync_cdek_tariffs.delay()
```
После успешного создания возвращается: 201 Created.

##### 2. Получение списка магазинов (GET /api/shops/)
При запросе списка:
1. Выполняется получение доступных магазинов.
2. Проверяются права доступа пользователя через `RolePermission`.
3. Результат сериализуется через `ShopSerializer`.

Схема работы:
```
GET  
│  
▼  
ShopViewSet.list()  
│  
▼  
Shop QuerySet  
│  
▼  
ShopSerializer  
│  
▼  
JSON
```

После успешного выполнения возвращается: 200 OK.

##### 3. Получение магазина по идентификатору (GET /api/shops/{id}/)
При запросе:
1. Выполняется поиск магазина.
2. Проверяются права доступа пользователя.
3. Магазин сериализуется и возвращается клиенту.

Схема работы:
```
GET  
│  
▼  
ShopViewSet.retrieve()  
│  
▼  
Shop  
│  
▼  
ShopSerializer  
│  
▼  
JSON
```

После успешного выполнения возвращается: 200 OK.

##### 4. Частичное обновление магазина (PATCH /api/shops/{id}/)
При обновлении:
1. Выполняется поиск магазина.
2. Проверяются права доступа пользователя.
3. Сохраняются текущие данные о службе доставки (`old_carrier`).
4. Обновляются переданные поля магазина.
5. Если транспортная компания изменилась:
   - вызывается `DeliveryFactory.cleanup(shop, old_carrier)` для очистки данных старой службы доставки;
   - вызывается `DeliveryFactory.initialize(shop)` для настройки новой службы доставки.

Схема работы:
```
PATCH  
│  
▼  
ShopViewSet.partial_update()  
│  
▼  
Получение старого carrier  
│  
▼  
ShopSerializer(partial=True)  
│  
▼  
UPDATE Shop  
│  
▼  
Проверка изменения carrier  
│  
├── carrier не изменился  
│ │  
│ ▼  
│ возврат данных магазина  
│  
└── carrier изменился  
│  
▼  
DeliveryFactory.cleanup(old_carrier)  
│  
▼  
DeliveryFactory.initialize(new_carrier)
```

После успешного выполнения возвращается: 200 OK.

##### 5. Удаление магазина (DELETE /api/shops/{id}/)
При удалении:
1. Выполняется поиск магазина.
2. Проверяются права доступа пользователя.
3. Вызывается `DeliveryFactory.cleanup(shop)`.
4. Фабрика выполняет очистку данных выбранной службы доставки.
5. Магазин удаляется из базы данных.

Схема работы:
```
DELETE  
│  
▼  
ShopViewSet.destroy()  
│  
▼  
DeliveryFactory.cleanup(shop)  
│  
▼  
cleanup_cdek(shop)  
│  
▼  
Удаление персональных настроек магазина  
│  
▼  
Проверка существования других магазинов СДЭК  
│  
├── Есть другие магазины  
│ │  
│ ▼  
│ завершение очистки  
│  
└── Последний магазин СДЭК  
│  
▼  
Удаление Redis-кэша  
│  
▼  
Удаление справочника тарифов
```

После успешного выполнения возвращается: 204 No Content.

##### Управление службами доставки через `DeliveryFactory` 
Для поддержки нескольких транспортных компаний используется фабрика:
DeliveryFactory.initialize(shop) и DeliveryFactory.cleanup(shop).
`ShopViewSet` не содержит условий вида:  "python if shop.carrier == DeliveryType.CDEK:", что позволяет добавлять новые службы доставки без изменения представления.

Для подключения новой службы достаточно зарегистрировать обработчики:
```python
_handlers = {
    DeliveryType.CDEK: initialize_cdek,
    DeliveryType.BOXBERRY: initialize_boxberry,
}
```
и:
```python
_cleanup_handlers = {
    DeliveryType.CDEK: cleanup_cdek,
    DeliveryType.BOXBERRY: cleanup_boxberry,
}
```

##### Пример обработки СДЭК

Создание первого магазина с СДЭК:
```
Shop
 │
 ▼
DeliveryFactory.initialize()
 │
 ▼
initialize_cdek()
 │
 ▼
Проверка существующих магазинов СДЭК
 │
 ▼
sync_cdek_tariffs.delay()
```

Удаление последнего магазина СДЭК:
```
Shop
 │
 ▼
DeliveryFactory.cleanup()
 │
 ▼
cleanup_cdek()
 │
 ▼
ShopDeliverySettingService.clear()
 │
 ▼
Удаление Redis-кэша
 │
 ▼
Удаление справочника тарифов CDEK
```

Такой подход позволяет централизовать управление интеграциями доставки и расширять систему новыми перевозчиками без изменения бизнес-логики магазина.


#### Эндпоинты личного кабинета магазина (`seller/views.py`)

##### 1. Получение выбранных тарифов магазина

```
GET /api/shops/{shop_id}/delivery-settings/
```

При открытии настроек магазина:

1. Определяется магазин текущего пользователя.
2. Получаются выбранные тарифы магазина через `ShopDeliverySettingService.get_shop_tariffs()`.
3. Связанные объекты тарифов загружаются с использованием `select_related("tariff")`.
4. Результат сериализуется и возвращается клиенту.

Схема работы:

```
GET
 │
 ▼
ShopDeliverySettingViewSet
 │
 ▼
ShopDeliverySettingService.get_shop_tariffs()
 │
 ▼
ShopDeliverySetting
        │
        └── select_related("tariff")
                     │
                     ▼
                CDEKTariff
                     │
                     ▼
Serializer
                     │
                     ▼
JSON
```


##### 2. Сохранение выбранных тарифов

```
POST /api/shops/{shop_id}/delivery-settings/
```

Запрос:

```json
{
    "tariffs": [499, 500]
}
```

Последовательность обработки:

```
Seller ЛК
      │
      ▼
ShopDeliverySettingViewSet
      │
      ▼
ShopDeliverySettingSerializer
      │
      ▼
ShopDeliverySettingService.save()
      │
      ├── Redis (проверка актуальности тарифов)
      ├── CDEKTariff
      └── ShopDeliverySetting
```

После успешного сохранения возвращается:

```
204 No Content
```


##### 3. Очистка выбранных тарифов

```
DELETE /api/shops/{shop_id}/delivery-settings/
```

При удалении настроек:

1. Определяется магазин текущего пользователя.
2. Вызывается метод `ShopDeliverySettingService.clear()`.
3. Удаляются все записи `ShopDeliverySetting`, связанные с данным магазином.

Схема работы:

```
DELETE
   │
   ▼
ShopDeliverySettingViewSet
   │
   ▼
ShopDeliverySettingService.clear()
   │
   ▼
DELETE FROM ShopDeliverySetting
WHERE shop_id = ...
```

После успешного удаления также возвращается:

```
204 No Content
```

##### Безопасность

Все эндпоинты настройки доставки защищены разрешениями **`IsCustomAuthenticated`** и **`RolePermission`**. Дополнительно при выполнении операций вызывается метод `get_user_shop()`, который проверяет принадлежность магазина текущему пользователю. Благодаря этому пользователь может просматривать и изменять настройки доставки только своего магазина.

### 2. Расчет доступных тарифов
(Модуль "Расчет стоимости доставки": метод ""Расчет по доступным тарифам и дополнительным услугам"")

Логика работы:
1. После добавления в корзину товаров, покупатель нажимает кнопку выбрать тариф:
и по каждой группе товаров, сформированной по продавцам, выходят доступные тарифы с доступными доп. услугами по каждому тарифу. Данные берутся:
```json
"type": 1,  
    "lang": "rus",  
    "from_location": {  
        "code": 270, // Код населенного пункта, установленый в ЛК продавца 
    },  
    "to_location": {  
        "code": 44,  // Код населенного пункта, установленый в ЛК покупателя
    },  
    "packages": [  
        {  
            "height": 10,  // из UniqueProduct
            "length": 10,  // из UniqueProduct
            "weight": 4000, // из UniqueProduct 
            "width": 10,    // из UniqueProduct
        }  
    ],  
    "services": services or [],  // из внутренних правил
}
```
 2. Покупатель нажимает на кнопку "Выбрать тариф": ему рассчитывается предварительная стоимость доставки.
3. покупатель нажимает кнопку: "Рассчитать стоимость корзины" и видит:

Общую стоимость товаров
     + 
Общую стоимость услуг по доставке
       =
Итоговая стоимость товаров (корзины)

Когда покупатель вводит адрес:
```
Cart
 |
 |
DeliveryFacade.calculate_delivery()
 |
 |
CDEKAdapter.get_available_tariffs()
```

Ответ:
```
[
  CDEK ПВЗ
  CDEK Курьер
  Экспресс
]
```

Нет информации ,какие доп. услуги применимы  к каким тарифам. Т.е. если доп.услуги н применимы, то Ап возвращат пустой словарь доп. услуг:
 Запрос:
 ```json
 services = [  
    {  
        "code": "TRYING_ON",  
        "parameter": "",  
    },  
    {  
        "code": "PART_DELIV",  
        "parameter": "",  
    },  
]
 ```
Ответ:
```json
{  
    "tariff_codes": [  
        {  
            "tariff_code": "121",  
            "status": "true",  
            "result": {  
                "delivery_sum": 1470.0,  
                "period_min": 0,  
                "period_max": 1,  
                "services": [  
                    {  
                        "code": "INSURANCE",  
                        "sum": 0.0  
                    }  
                ],  
                "total_sum": 1764.0,  
                "currency": "RUB"  
            }  
        },  
        {  
            "tariff_code": "59",  
            "status": "true",  
            "result": {  
                "delivery_sum": 2790.0,  
                "period_min": 1,  
                "period_max": 1,  
                "services": [  
                    {  
                        "code": "INSURANCE",  
                        "sum": 0.0  
                    }  
                ],  
                "total_sum": 3348.0,  
                "currency": "RUB"  
            }  
        }  
    ]  
}
```

Можно ориентироваться по таблице в документации СДЭКа - Приложение 6. Дополнительные услуги
### 3. Расчет выбранного тарифа
(Модуль "Расчет стоимости доставки": метод "Расчет по коду тарифа")

Осуществляется после выбора покупателем конкретного тарифа.

### 4. Создание заказа
(Модуль "Заказ": метод "Регистрация заказа" )

Схема:
```
post_order()
       |
       |
HTTP 200
       |
       |
get_order_uuid()
       |
       |
проверка requests.state
```

### 5. Получение статуса

После успешного создания:
```
CDEK UUID
     |
     |
получаем cdek_number
     |
     |
отслеживаем
```

Итоговая схема:
```
Покупатель оформляет заказ
          |
          |
Создается Order
          |
          |
Создается OrderDelivery
(разбиение заказа по продавцам / магазинам)
          |
          |
Рассчитываются доступные тарифы для каждого OrderDelivery
          |
          |
Покупатель выбирает тариф
(Сохраняется выбранный тариф в CdekDeliveryMeta)
          |
          |
Регистрация CDEK заказа (post_order)
          |
          |
Получение информации по UUID (get_order_uuid)
          |
          |
          OK (Если доставка создана успешно)
          |
          |
Оплата покупателем
          |
          |
Передача заказа продавцу
```
